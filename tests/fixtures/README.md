# Fixtures

Use this directory for small HTML fixtures that exercise validation scripts.

Recommended groups:

- `valid-*`: examples expected to pass.
- `invalid-*`: examples expected to fail with known rule violations.
